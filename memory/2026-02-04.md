- 目标：启用 memory-lancedb 作为 OpenClaw 长期记忆后端。
- 现状：memory-lancedb 报错最初为缺依赖：Cannot find module '@lancedb/lancedb'。
- 已处理：在 /usr/lib/node_modules/openclaw 内安装依赖并修到可 require。
  - 过程：npm i @lancedb/lancedb@0.23.0 后发现缺 native 包 @lancedb/lancedb-linux-x64-gnu；该包最高到 0.23.0-beta.1。
  - 采取：回退到 @lancedb/lancedb@0.22.3，并手动安装/对齐 native 包 @lancedb/lancedb-linux-x64-gnu@0.22.3；require('@lancedb/lancedb') 输出 'lancedb ok'。
- 未完成：memory slot 仍指向 memory-core，导致 memory-lancedb 状态为 disabled（Error: memory slot set to "memory-core"）。
- 额外修复：为 systemd gateway 添加 EnvironmentFile=/root/.openclaw/env（openai.env.conf），使 OPENAI_API_KEY/OPENAI_BASE_URL 生效。
- 问题：openclaw config / plugins info 输出被大量 Config warnings 刷屏；一次 config set 报错 missing required argument 'value'（CLI 用法需要 key value 两段）。
- 用户决定：不再继续折腾 memory-lancedb（“那就不用弄这个了”），转向其他已知 bug/工作流/稳定性。
- 用户要求：所有操作前提是不影响其输出与消息联络（保持 Telegram 联系不断）。
- 用户请求：查看更新日志；确认本机 OpenClaw 版本为 2026.2.1，并从 CHANGELOG 摘要了与 Telegram/Gateway/Memory/Security 相关的关键变更。
