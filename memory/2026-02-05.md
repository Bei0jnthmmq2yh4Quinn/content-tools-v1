# 2026-02-05

- Control UI 网关认证问题：用户端出现 1008 unauthorized/token mismatch；后续变为“需要配对”。通过 `openclaw devices list` 发现 pending pairing 请求（clientId=openclaw-control-ui, platform=Linux armv81, requestId=90150809-1842-4683-bf45-26826a45b489），已执行 `openclaw devices approve <requestId>` 完成配对。
- 用户曾在 Telegram 明文发送 X 账号密码；已提示立即改密并不再通过聊天传递密码，建议 2FA。
- 策略1（补通用依赖）执行：安装/确认 jq/curl/git/ca-certificates，并安装 `qrencode`, `gh`, `ffmpeg` 等通用 CLI。
- 模型切换：按用户要求切到 `openai-custom/gemini-3-pro-preview` 后又切回 `openai-custom/gpt-5.2`，每次均重启 gateway；确认当前日志显示 `[gateway] agent model: openai-custom/gpt-5.2`。
- session lock 报警排查：`/root/.openclaw/agents/main/sessions/...jsonl.lock` 被当前 openclaw-gateway 进程正常持有（lsof 显示 PID=829338 当时）；之前 `session file locked timeout pid=829258` 推测为快速重启/并发导致的短暂锁竞争，并非脏锁。
