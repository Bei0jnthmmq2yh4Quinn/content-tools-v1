# 2026-02-12

## xiaohongshu-mcp（MCP）状态确认
- 用户：xqp 说“MCP 配置好了，让我看一下”。
- 本机进程：`./xiaohongshu-mcp-linux-amd64` 在跑（ps 看到常驻）。
- 连通性：`http://127.0.0.1:18060/` 根路径返回 404（属正常表现，不代表服务挂）。
- Python 客户端测试：系统无 `python` 命令，需用 `python3`（3.11.2）。
- `python3 skills/xiaohongshu-mcp/scripts/xhs_client.py status` 返回：`✅ Logged in as: xiaohongshu-mcp`。

## 备注
- 后续如要做功能验证：优先用 `xhs_client.py search/detail/publish` 做端到端测试。
