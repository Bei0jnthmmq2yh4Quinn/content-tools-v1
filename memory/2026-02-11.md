# 2026-02-11

## 003636 API key 问题
- 旧 key `sk-ALcX...UAVw` 一度报 401，后恢复正常（003636 临时波动）
- 新 key `sk-coiu...OAaD` 能访问 /v1/models 但**没有 gpt-5.2 权限**（403），只能访问部分模型
- 结论：继续用旧 key，新 key 仅作备用（权限不足）
- anyrouter key: `sk-VYaM...k9BN`

## 群聊 401 排查
- 群聊 session 用 `openai-responses` API 调 gpt-5.2，和私聊一样的 key
- 根因是 003636 临时波动，恢复旧 key 后正常

## 小红书 MCP
- 服务运行在 localhost:18060，健康
- 已登录，用户名: xiaohongshu-mcp
- MCP v2.0.0，13 个 tools: check_login_status, search_feeds, publish_content, publish_with_video, get_feed_detail, list_feeds, like_feed, favorite_feed, post_comment_to_feed, reply_comment_in_feed, user_profile, get_login_qrcode, delete_cookies
- xhs_client.py 脚本用的是旧 HTTP API，实际 MCP server 用 JSON-RPC over /mcp/v1
